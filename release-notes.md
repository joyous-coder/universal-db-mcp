# v4.0.3 Release Notes

**发布日期**: 2026-08-18
**类型**: Bug 修复 + 性能优化
**npm**: `@joyous-coder/universal-db-mcp@4.0.3`

## 摘要

修复了 **DM/Oracle 适配器 2 个新 bug** (Bug #15 + Bug #17) + **性能优化** (`get_table_info` 提速 50-100 倍)。

兼容 4.0.0 API,无需迁移。

## 🐛 Bug 修复

### Bug #15: DM `get_table_info` 失败 (Oracle/DM 兼容)

`get_table_info('V403_DM.T_USERS')` 在达梦上提示"表不存在"。两个根因:

1. `ALL_IND_COLUMNS` join 用 `ic.IND_OWNER` (错的) → 修正为 `ic.INDEX_OWNER`
2. 裸名表无 owner → fallback `owner = this.config.user`

### Bug #17: `generate_sample_data` 主键处理

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| IDENTITY PK (`GENERATED BY DEFAULT AS IDENTITY`) | bind int → "表或视图不存在" | 自动跳过,DB 自填 |
| VARCHAR2(36) UUID PK | INSERT 不含 id → ORA-01400 | 生成 uuid v4 |
| 非 IDENTITY INT PK (MAX=300) | 生成 1-100000 随机 int → 冲突 | `MAX(pk) + rowIndex + 1` |
| Oracle `NUMBER` 列 | fallback lorem → ORA-01722 | 生成 int |
| DM `TIMESTAMP` 列 | bind JS Date → 类型异常 | ISO 字符串 |

## ⚡ 性能

| 操作 | 修复前 | 修复后 |
|------|--------|--------|
| `get_table_info` (Oracle) | 60-90s (full schema scan) | 526-866ms (4 SQL single-table) |
| `get_table_info` (DM) | 30-60s | 696-852ms |
| `get_sample_data` / `get_enum_values` | 30-60s | ~150ms |

## 📦 安装

```bash
npm install @joyous-coder/universal-db-mcp@4.0.3
```

## 🧪 测试报告

- Oracle 17c: `docs/04-reports/2026-08-17-oracle-tool-validation.md` (41 tools, 27 PASS / 2 caveat / 0 FAIL)
- DM 8: `docs/04-reports/2026-08-18-dm-tool-validation.md` (41 tools, 27 PASS / 1 caveat / 0 FAIL)

## 完整改动

```
790edf6 fix(generator): v4.0.3.2 — Bug #17 UUID PK + CHAR(36)/VARCHAR(36) PK
2e50a40 fix(generator): v4.0.3.2 — Bug #17 MAX(pk) sequence + Oracle NUMBER + Oracle IDENTITY
a47af1f fix(dm): v4.0.3.2 — Bug #17 generate_sample_data IDENTITY PK
4941565 fix(dm): v4.0.3 — Bug #15 getTableInfo INDEX_OWNER + owner fallback
4cd5d03 perf(getTableInfo): v4.0.3.1 — remove per-table cache (cold 866ms is fine)
cd9d581 perf(getTableInfo): v4.0.3 — per-table adapter-direct + cache (Oracle 86s → 593ms)
```